import React from "react"
import styles from "./Selector.module.scss"

const Selector: React.FC = () => {
    return <div className={styles.container}>{/* <input type /> */}</div>
}

export default Selector
